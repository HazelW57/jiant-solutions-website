# Jiant Solutions Website

Static company website for GitHub Pages.

## Deployment

Enable GitHub Pages from the `main` branch and root directory. The custom domain is configured as `www.jiantsolutions.com` through the `CNAME` file.

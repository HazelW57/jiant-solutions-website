const button=document.querySelector('.menu');const nav=document.querySelector('header nav');button.addEventListener('click',()=>nav.classList.toggle('open'));nav.addEventListener('click',()=>nav.classList.remove('open'));
